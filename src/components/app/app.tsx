import styles from './app.module.css';

const App = () => {
  return (
    <main className={styles.container}>
      <h1 className={styles.title}>Firebase to-do app</h1>
    </main>
  );
};

export default App;
